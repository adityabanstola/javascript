first.className = "yellow"// if we assign in element.className , it replaces the whole stting of a class

first.classList
first.classList.remove("yellow")// element.classList.remove/add remove or add a class respectively
//element.classList.toggle adds the class if it does not exists, otherwise remove it
first.classList.contains("red")// element.classList